// Package app implements the mogo-tester command.
//
// It parses CLI options, discovers Mojo test files from directories and direct
// file operands, compiles each file with mojo build, runs the resulting
// binaries, and reports per-file and aggregate results.
package app
