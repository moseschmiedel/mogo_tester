// Package app implements the mogo-tester command.
//
// It parses CLI options, discovers top-level Mojo test files, compiles each
// file with mojo build, runs the resulting binaries, and reports per-file and
// aggregate results.
package app
